@model ManvarFitness.Entity.AdminUser
@{
    ViewData["Title"] = "Delete User";
}
<h1>Delete User</h1>

<div class="alert alert-danger">
    Are you sure you want to delete user <strong>@Model.EmailUsername</strong>?
</div>

<form method="post" action="/AdminUser/Delete">
    @Html.AntiForgeryToken()

    <input type="hidden" name="id" value="@Model.Id" />

    <button type="submit" class="btn btn-danger">Delete</button>
    <a class="btn btn-secondary" href="/AdminUser/Index">Cancel</a>
</form>
